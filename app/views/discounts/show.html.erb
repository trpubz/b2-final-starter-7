<%= render partial: "shared/nav" %>

<body>
  <div class="row">
    <p class='col-12'>Bulk Discount: <%= @discount.id %></p>
  </div>

  <p><%= @discount.discount.to_discount_format %></p>
  <p>Min Qty: <%= @discount.min_qty %></p>
  <%= button_to "Edit", edit_merchant_discount_path(@merchant, @discount), method: :get %>
</body>